{
  "properties": {
    "ano_id": {
      "bsonType": "string",
      "minLength": 3,
      "maxLength": 64,
      "pattern": "^[a-z0-9-]{3,64}$",
      "title": "ID",
      "description": "ID",
      "default": "database-dump"
    },
    "ano_vie_id": {
      "bsonType": "string",
      "minLength": 3,
      "maxLength": 64,
      "pattern": "^[a-z0-9-]{3,64}$",
      "title": "ID",
      "description": "View ID"
    },
    "ano_vie_title": {
      "bsonType": "string",
      "minLength": 1,
      "maxLength": 64,
      "title": "Title",
      "description": "View Title"
    },
    "ano_scope": {
      "bsonType": "string",
      "title": "Type",
      "description": "Type"
    },
    "ano_to": {
      "bsonType": "array",
      "uniqueItems": false,
      "minItems": 0,
      "maxItems": 256,
      "items": {
        "bsonType": "string",
        "minLength": 0,
        "maxLength": 64
      },
      "title": "To",
      "description": "To List",
      "manualAdd": false
    },
    "_tags": {
      "bsonType": "array",
      "uniqueItems": false,
      "minItems": 0,
      "maxItems": 256,
      "items": {
        "bsonType": "string",
        "minLength": 2,
        "maxLength": 64,
        "pattern": "^[#@][a-zA-Z0-9]{2,64}$"
      },
      "title": "Tags",
      "description": "Enter a tag #...",
      "manualAdd": false
    }
  },
  "required": ["ano_id", "ano_scope", "ano_vie_id", "ano_vie_title"],
  "unique": ["ano_id"],
  "index": [["ano_scope"], ["ano_vie_id"]],
  "sort": {
    "_created_at": -1
  }
}
